apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
secretGenerator:
  - name: loki-values-base
    namespace: observability
    type: Opaque
    files:
      - values.yaml=helm-values/values-v7.3.0.yaml
    options:
      disableNameSuffixHash: true
